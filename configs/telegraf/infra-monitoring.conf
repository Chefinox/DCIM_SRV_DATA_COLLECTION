# --- Infra Self-Monitoring (Phase 3.1) ---

# 1. Elasticsearch Health
[[inputs.elasticsearch]]
  servers = ["https://10.70.0.56:9200"]
  username = "elastic"
  password = "C+H+pFb*aIAqWcOo-X8q"
  insecure_skip_verify = true
  cluster_health = true
  cluster_stats = true

# 2. PostgreSQL Stats
[[inputs.postgresql]]
  address = "postgres://sot_admin:Inovasi%400918@10.70.0.56:5432/postgres?sslmode=disable"
  databases = ["dcim_sot"]

# 3. Output infra metrics to Elasticsearch directly
[[outputs.elasticsearch]]
  urls = ["https://10.70.0.56:9200"]
  username = "elastic"
  password = "C+H+pFb*aIAqWcOo-X8q"
  index_name = "dcim-infra-metrics-%Y.%m.%d"
  insecure_skip_verify = true
  # Only route infrastructure metrics to ES (prevent DCIM raw metrics from going here)
  namepass = [
    "elasticsearch_*",
    "postgresql",
    "cpu", "mem", "disk", "diskio", "system", "processes", "kernel"
  ]
