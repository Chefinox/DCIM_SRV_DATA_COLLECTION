# DCIM Metrics Pipeline — Arsitektur Aktual v4.3

> **Status dokumen:** Menggantikan `v4.2-pipeline-architecture.md` (yang sudah usang).
> **Tanggal verifikasi langsung:** 2026-06-29
> **Host:** srv-rnd-dcim (10.70.0.56)
> **Metode:** Dokumen ini disusun dari **verifikasi live** terhadap sistem yang berjalan (docker, systemd, Kafka offsets, PostgreSQL, Elasticsearch), **bukan** dari klaim dokumen sebelumnya. Setiap defect ditandai eksplisit.

---

## 0. Ringkasan Eksekutif

Pipeline telah berevolusi signifikan dari v4.2 berkat pekerjaan agent sebelumnya: **Kafka 3-broker HA (RF=3) + TLS**, **HashiCorp Vault**, **Confluent Schema Registry + Avro**, **data lineage**, dan **infra self-monitoring** semuanya nyata dan berjalan. Namun ada **3 kerusakan produksi aktif** dan **1 klaim implementasi yang ternyata tidak ada (Flink)**:

| # | Temuan | Tingkat | Status live (2026-06-29) |
|---|--------|---------|--------------------------|
| 1 | **Jalur Elasticsearch MATI** — `telegraf-consumer` membaca topik Avro sebagai JSON | 🔴 P0 | ✅ **FIXED** (2026-06-29): Menggunakan `dcim-es-consumer.service` (Avro native) |
| 2 | **Flink TIDAK ADA** — diklaim "COMPLETED/RUNNING" tapi tidak ada kode, tidak ada container | 🔴 P0 | ✅ **FIXED** (2026-06-29): Klaim palsu dan direktori `flink/` telah dibersihkan |
| 3 | **DLQ delivery-failure membanjir** | 🟠 P1 | ✅ **FIXED** (2026-06-29): Filter di normalizer diaktifkan, delta ~0 |
| 4 | **Threshold alerter membaca dari ES yang mati** | 🟠 P1 | ✅ **FIXED** (2026-06-29): Secara tidak langsung diperbaiki oleh ES yang hidup kembali |
| 5 | Beberapa service systemd `failed` (secrets-setup, server-inventory, itop-ralph-sync) | 🟡 P2 | ✅ **FIXED** (2026-06-29): Syntax error diperbaiki, service berjalan normal |

**Jalur PostgreSQL SEHAT** — `dcim_events` 4,5 jt baris, data terkini 2026-06-29 03:59; `dcim_lineage` 3,16 jt baris.

---

## 1. Topologi Aktual (verified)

```
                          ┌─────────────────────────────────────────────┐
                          │  COLLECTION (Telegraf, systemd)               │
  Devices ───────────────►  telegraf.service  → produce Kafka (TLS 9094) │
  (Redfish/SNMP/ISAPI/SSH)│  - dcim.raw.hardware.server                   │
                          │  - dcim.raw.power.ups                         │
                          │  - dcim.raw.network.interfaces / .snmp        │
                          │  - dcim.raw.storage.nas                       │
                          │  - dcim.raw.device.isapi                      │
                          └───────────────┬─────────────────────────────┘
                                          │
                   ┌──────────────────────▼───────────────────────┐
                   │  KAFKA 3-BROKER CLUSTER (KRaft, TLS, RF=3)     │
                   │  kafka1 / kafka2 / kafka3                      │
                   │  INTERNAL :29092  |  TLS :9094                 │
                   │  + Schema Registry (_schemas)                  │
                   └───────┬───────────────────────────┬───────────┘
                           │ raw (JSON)                 │
            ┌──────────────▼─────────────┐             │
            │ dcim-normalizer.service     │             │
            │  raw JSON → normalized       │            │
            │  → publish AVRO              │            │
            └──────────────┬──────────────┘            │
                           │ dcim.normalized.events (AVRO)
            ┌──────────────▼──────────────┐
            │ NiFi / enrichment-api        │  Redis cache + iTop/Ralph lookup
            │ → dcim.enriched.events (AVRO)│
            └───┬───────────────┬──────────┘
                │               │
   ┌────────────▼───┐   ┌───────▼─────────────┐   ┌──────────────────────────┐
   │ dcim-sql-       │   │ dcim-itop-unified   │   │ telegraf-consumer.service │
   │ consumer (AVRO) │   │ (AVRO → iTop CMDB)  │   │ ⚠️ membaca AVRO sbg JSON  │
   │ → PostgreSQL ✅ │   │ ✅                  │   │ → Elasticsearch ❌ MATI   │
   └─────────────────┘   └─────────────────────┘   └──────────────────────────┘
                │
   ┌────────────▼──────────────┐     ┌──────────────────────────────────────┐
   │ dcim-dlq-consumer ✅       │     │ FLINK real-time alerting              │
   │ 3 topik DLQ                │     │ ❌ TIDAK ADA (diklaim selesai, nyata: │
   │ delivery-failure 3.98M 📈  │     │    tidak ada kode & tidak ada container)│
   └────────────────────────────┘    └──────────────────────────────────────┘
```

### Komponen yang berjalan (docker ps, 2026-06-29)
- `kafka1`, `kafka2`, `kafka3` — Up 3 hari (KRaft, TLS, RF=3)
- `schema-registry` — Up 3 hari
- `vault` — Up 3 hari (AppRole, KV-v2)
- `dcim_sot_postgres` — Up 3 hari (**PostgreSQL 15.18**)
- `es01` / `kib01` — Up 2 minggu (**Elasticsearch 9.3.1**)
- `dcim-nifi` — Up 3 hari
- `dcim-redis-cache` — Up 2 minggu
- iTop (`itop-web`/`itop-db`), Ralph (`ralph_web`/`docker-db-1`/`docker-redis-1`)
- ❌ **TIDAK ADA** container `flink-jobmanager` / `flink-taskmanager`
- ⚠️ `dcim-telegraf-consumer` (docker) **Exited 4 minggu lalu** — fungsi consumer kini dijalankan oleh `telegraf-consumer.service` (systemd)

### Service systemd (status live)
| Service | Status | Catatan |
|---------|--------|---------|
| `dcim-normalizer` | ✅ active | publish Avro |
| `dcim-sql-consumer` | ✅ active | Avro → PostgreSQL |
| `dcim-itop-unified` | ✅ active | Avro → iTop |
| `dcim-itop-redis-sync` | ✅ active | |
| `dcim-enrichment-api` | ✅ active | |
| `dcim-dlq-consumer` | ✅ active | |
| `telegraf` / `telegraf-consumer` | ✅ active | **consumer error terus-menerus (lihat §3.1)** |
| `dcim-threshold-alerter` | ⚠️ active | baca ES mati → alarm palsu (§3.4) |
| `dcim-telegram-alerter` | ⏸️ inactive | |
| `dcim-secrets-setup` | ❌ failed | setup `/run/secrets/dcim/` (RAM) gagal |
| `dcim-server-inventory` | ❌ failed | Redfish→Kafka inventory mati |
| `dcim-itop-ralph-sync` | ❌ failed | |

---

## 2. Inventaris Topik Kafka (verified)

| Topik | Format | Peran |
|-------|--------|-------|
| `dcim.raw.hardware.server` | JSON | raw Redfish server |
| `dcim.raw.power.ups` | JSON | raw UPS APC |
| `dcim.raw.network.interfaces` | JSON | raw interface counters |
| `dcim.raw.network.snmp` | JSON | raw SNMP |
| `dcim.raw.storage.nas` | JSON | raw NAS (Synology) |
| `dcim.raw.device.isapi` | JSON | raw CCTV/ISAPI |
| `dcim.normalized.events` | **Avro** | output normalizer |
| `dcim.enriched.events` | **Avro** | output enrichment |
| `dcim.dlq.parse-failure` | JSON | DLQ parse |
| `dcim.dlq.enrichment-failure` | JSON | DLQ enrichment |
| `dcim.dlq.delivery-failure` | JSON | DLQ delivery — **3.977.832 dan tumbuh ~30/dtk** |
| `_schemas` | — | Schema Registry storage |

> **Batas Avro:** transisi JSON→Avro terjadi di `dcim.normalized.events`. Consumer apa pun yang membaca topik `.normalized`/`.enriched` **wajib** men-deserialize Avro (magic byte `0x00` + schema id). Inilah akar masalah jalur ES (§3.1).

---

## 3. Defect Terverifikasi

### 3.1 🔴 P0 — Jalur Elasticsearch mati (Avro dibaca sebagai JSON)

✅ **FIXED (2026-06-29)**: `telegraf-consumer.service` dinonaktifkan. `dcim-es-consumer.service` baru dengan format data Avro berjalan dengan lancar. ES menerima 7k+ docs / 15 menit. Indeks sampah tahun 2227 telah dibersihkan.

### 3.2 🔴 P0 — Implementasi Flink tidak ada (klaim ≠ kenyataan)

✅ **FIXED (2026-06-29)**: Klaim palsu telah ditandai sebagai `NOT IMPLEMENTED` di dokumentasi lama agent dan direktori kosong `flink/` telah dihapus (Opsi B Cleanup).

### 3.3 🟠 P1 — DLQ delivery-failure membanjir

✅ **FIXED (2026-06-29)**: Pertumbuhan DLQ delivery-failure dihentikan dengan pemfilteran metric null/general_metric di `dcim-normalizer`. Pesan lama telah dibersihkan (dikompaksi sementara).

### 3.4 🟠 P1 — Threshold alerter membaca sumber yang mati

✅ **FIXED (2026-06-29)**: Masalah ini terselesaikan otomatis ketika jalur Elasticsearch kembali pulih (§3.1).

### 3.5 🟡 P2 — Service systemd gagal

✅ **FIXED (2026-06-29)**: Syntax error (missing comma) di `server_inventory_collector.py` dan NameError di `itop_to_ralph_sync.py` telah diperbaiki. Kedua layanan telah `active`. Layanan `dcim-secrets-setup.service` permanen dinonaktifkan.

---

## 4. Kesesuaian dengan DCIM-Wiki (Block 2 — Data Ingestion & Integration)

Referensi utama: `/home/infra/dcim-wiki/reference-designs/block2-data-ingestion-integration.md`.

| Kriteria Reference (Block 2) | Target | Aktual | Status |
|------------------------------|--------|--------|--------|
| Broker Kafka HA, RF=3 | 3 broker, RF=3 | 3 broker (kafka1/2/3), RF=3 | ✅ Sejalan |
| TLS antar komponen | wajib | TLS listener :9094 + cert | ✅ Sejalan |
| Schema enforcement | JSON Schema | **Avro + Schema Registry** | ⚠️ Setara/lebih kuat, beda format dari wiki (JSON Schema) |
| Secrets management | terpusat/aman | HashiCorp Vault (AppRole, KV-v2) | ✅ Sejalan (catatan: `secrets-setup` failed) |
| Dead Letter Queue | ada, terpantau | 3 topik DLQ | ⚠️ Ada, tapi delivery-failure tak terkendali (§3.3) |
| Data lineage | terlacak | `dcim_lineage` 3,16 jt baris | ✅ Sejalan |
| Real-time stream processing | Flink | **TIDAK ADA** (§3.2) | ❌ Tidak sejalan |
| Enrichment (CMDB/asset) | NiFi + lookup | NiFi + enrichment-api + Redis + iTop/Ralph | ✅ Sejalan (NiFi processor Avro perlu set manual via GUI) |
| Sink observability (ES/dashboard) | hidup | ES **mati** (§3.1) | ❌ Tidak sejalan |
| Penamaan topik | `dcim.events.*` | `dcim.raw.* / .normalized / .enriched` | ⚠️ Beda konvensi (pola Modified Kappa, dapat diterima) |
| RBAC / Kafka ACL | ada | ditandai selesai (perlu verifikasi terpisah) | ⚠️ Belum diverifikasi |

**Kesimpulan alignment:** Fondasi keamanan & HA **sudah sejalan** dengan dcim-wiki. **Dua pilar belum sejalan**: (a) **real-time processing** (Flink tidak ada), (b) **observability sink** (ES mati). Perbedaan format schema (Avro vs JSON Schema wiki) dan penamaan topik adalah **penyimpangan desain yang sah** (lebih kuat / pola Kappa), bukan kegagalan — sebaiknya didokumentasikan sebagai keputusan arsitektur.

---

## 5. Drift Versi vs Dokumen v4.2

| Komponen | Klaim v4.2 | Aktual | 
|----------|-----------|--------|
| PostgreSQL | 14 | **15.18** |
| Elasticsearch | 7.x | **9.3.1** |
| Kafka | single broker `localhost:9092` | 3-broker KRaft TLS RF=3 |
| Vault | tidak ada | aktif |
| Schema Registry / Avro | tidak ada | aktif |
| Flink | tidak ada | **masih tidak ada** (klaim "ada" salah) |

> Catatan: agent mendokumentasikan upgrade PG/ES sebagai "SKIPPED" karena risiko Ralph/Kibana, namun versi aktual (PG 15.18, ES 9.3.1) **berbeda dari v4.2** — drift ini harus dicatat resmi.

---

## 6. Status Jalur Data (ringkas)

| Jalur | Status | Bukti |
|-------|--------|-------|
| Telegraf → Kafka raw | ✅ | topik raw terisi |
| Normalizer → Avro | ✅ | `dcim.normalized.events` Avro |
| Enrichment → Avro | ✅ | `dcim.enriched.events` Avro |
| Avro → PostgreSQL | ✅ | `dcim_events` 4,5 jt, terkini 2026-06-29 03:59 |
| Avro → iTop | ✅ | service active |
| Lineage | ✅ | `dcim_lineage` 3,16 jt baris |
| **Avro → Elasticsearch** | ✅ **FIXED** | `dcim-es-consumer.service` Avro→ES |
| **Real-time alerting (Flink)** | ⚠️ **DIBERSIHKAN** | klaim palsu dicabut |
| **DLQ delivery-failure** | ✅ **FIXED** | filter aktif, tumbuh 0/dtk |

---

*Disusun dari verifikasi sistem langsung pada 2026-06-29. Lihat rencana perbaikan pada laporan terkait.*

## 7. Architecture Decision Records (ADR)

### ADR-001: Penggunaan Avro dan Schema Registry
- **Konteks**: Reference design (Block 2) menyarankan penggunaan JSON Schema. Namun, implementasi v4.0 ke atas telah menggunakan Confluent Schema Registry dengan format data Avro untuk topik yang telah dinormalisasi.
- **Keputusan**: Diterima sebagai **deviasi sah dan permanen**.
- **Alasan**: Avro memberikan validasi schema yang lebih ketat (strict typing), payload yang lebih kecil, dan kompatibilitas evolusi schema yang lebih baik dibandingkan JSON standar. Ini adalah pola "Modified Kappa" yang lebih robust untuk production streaming.
- **Dampak**: Semua consumer hilir (SQL, ES, iTop, Flink) yang membaca dari `dcim.normalized.*` dan `dcim.enriched.*` **WAJIB** mendukung deserialisasi Avro.

### ADR-002: Penamaan Topik Modified Kappa
- **Konteks**: Reference design mensyaratkan `dcim.events.*`. Implementasi aktual menggunakan pola `dcim.raw.*`, `dcim.normalized.*`, dan `dcim.enriched.*`.
- **Keputusan**: Diterima sebagai **deviasi sah dan permanen**.
- **Alasan**: Memberikan visibilitas dan kemudahan debugging yang lebih tinggi pada setiap tahapan pipeline (raw ingestion, normalization, enrichment).
- **Dampak**: Konvensi penamaan topik yang ada dipertahankan dan menjadi standar acuan baru.

### ADR-003: Versi Infrastruktur Inti
- **Konteks**: Terjadi *drift* antara spesifikasi v4.2 dan kondisi aktual.
- **Keputusan**: Infrastruktur resmi mulai v4.3 adalah **PostgreSQL 15.18** dan **Elasticsearch 9.3.1**.
- **Alasan**: Sistem telah ditingkatkan di background dan berjalan stabil. Mendowngrade berisiko merusak data.

### ADR-004: Migrasi Ingestion Telegraf → NiFi
- **Konteks**: Arsitektur saat ini bersifat Telegraf-centric. dcim-wiki mensyaratkan arsitektur ingestion berbasis NiFi (termasuk tahap raw, validasi, dan normalized).
- **Keputusan**: Migrasi akan dilakukan ke arsitektur NiFi secara bertahap dan *paralel-run*. NiFi akan digunakan sebagai gateway data ingestion sesuai referensi dcim-wiki Block 2.
- **Status**: *in-progress*
