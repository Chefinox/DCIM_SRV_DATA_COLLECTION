logs/*.log*
logs/*.gz
logs/*.1
.venv*/
tmp_*
scripts/tmp_*

# AI integration secrets (never commit)
configs/ai_reader.credentials
sql/*.local
configs/.env

# Vault secrets (never commit)
vault/config/init.txt
vault/config/role_id
vault/config/secret_id
__pycache__/
*.pyc
logs/*.json
itop/itop/log/*

# Cloned repos (prevent double-tracking)
DCIM_SRV_DATA_COLLECTION/
